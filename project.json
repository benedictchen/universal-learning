{
  "name": "universal_learning",
  "root": "packages/universal_learning", 
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["python -m pip install build", "python -m build"],
        "cwd": "packages/universal_learning"
      }
    },
    "test": {
      "executor": "nx:run-commands", 
      "options": {
        "commands": ["python -m pytest . -v"],
        "cwd": "packages/universal_learning"
      }
    }
  }
}
